const db = require("../../data/db-config");

module.exports = {
  getAll,
};

function getAll() {
  return null;
}

function getById(id) {
  return null;
}

function create(object) {
  return null;
}

function update(id, changes) {
  return null;
}

function remove(id) {
  return null;
}
